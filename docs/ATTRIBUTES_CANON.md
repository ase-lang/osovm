# Ọ̀ṢỌ́ Attribute Canon - 143 Sacred Attributes

## Overview

The full canon of 143 attributes enables universal device support across:
- **SimaaS**: Simulation-as-a-Service for testing non-human entities
- **AIO**: Universal job marketplace (digital, meta-digital, physical)
- **TechGnØŞ.EXE**: Spiritual tech framework with 50/25/15/10 shrine splits
- **Òrìṣà ROBOTS**: Autonomous entities funded 100% by offerings

## Categories

### 1-10: Core Proof & Witness
- `@proof(kind:proof_kw, receipt:hash64)` - Validates real-world action
- `@witness(int)` - Requires witness count
- `@receipt(kind, hash, pointer, verifiers)` - Composite tracking
- `@àṣẹ(proof, witnesses, qr?, auto_device?)` - Universal seal
- `@telemetry(device:str)` - Device telemetry stream

### 11-20: SimaaS & AIO Jobs
- `@sim(episode_id, steps)` - Runs simulation episodes
- `@job(type, title, budget, requires)` - Posts universal jobs
- `@hardware(estop_check)` - Enforces safety (e.g., E-stop)
- `@collaboration(actors, roles)` - Multi-actor coordination
- `@accountability(metric)` - Tracks "who drove what"
- `@offering(amount, shrine)` - TechGnØŞ shrine offerings
- `@sim_fund(user_fee, rate_limit)` - Funds simulations
- `@circuit_breaker(max_attempts, cooldown_ms)` - Pauses failures

### 21-30: Herb & Regional (Òsanyìn)
- `@herb(key, class)` - Tags herbs (e.g., ONCO, FIRE)
- `@regional(locale)` - Prioritizes regional herbs (e.g., "DO-Santiago")

### 31-40: Governance & Safety
- `@vote(weights, quorum)` - Weighted voting
- `@dispute(window, quorum)` - Dispute resolution
- `@slash(amount, reason)` - Penalizes failed jobs
- `@wallet(balance, stake, caps, policy)` - Manages actor wallets
- `@identity(kind, issuer)` - Verifiable identities (DID, ENS, SatName)
- `@swarm(plan)` - Swarm intelligence coordination
- `@peer(target, task)` - Assigns peer tasks
- `@flow_validate(strict)` - Validates data flows

### 41-50: Native VM Extensions
- `@memory(kind, index, pointer)` - Persists state (ephemeral/durable/vector/graph)
- `@learn(schedule, from, method)` - Trains from receipts (RLHF/batch)
- `@compute(gpu, hours, provider)` - Allocates GPU resources
- `@verifier(role, quorum)` - Assigns verifiers (steward/witness/council)
- `@multisig(quorum, signers)` - Multi-signature approval
- `@policy(rule)` - Enforces governance (min_stake, hardware_safety, etc.)
- `@council(quorum, role)` - Defines councils (tech/spirit/safety)
- `@beats(beat_ms, microbatch, prefill, kv_warm_ms)` - Timing cadence
- `@maintenance(day)` - Schedules maintenance
- `@cog(memory)` - Sets cognition memory mode
- `@npc(wallet)` - Binds NPC wallets for non-humans

### 51-70: Entertainment Sector (Generalized for Jobs)
- `@project(id, sector, budget, milestones)` - Defines projects
- `@milestone(id, amount, due, deliverables)` - Tracks milestones
- `@casting(id, role, dayRate)` - Assigns actors
- `@callsheet(id, projectId, shootDate, callTimes)` - Schedules tasks
- `@release(id, kind, fileHash)` - Records deliverables
- `@license(id, workId, rights, term)` - Grants usage rights
- `@royalty(id, triggers, splits)` - Distributes royalties
- `@safety(id, items, signedByVisa)` - Safety protocols
- `@permit(id, authority, validFrom, validTo)` - Regulatory permits
- `@delivery(id, uri, checksum, state)` - Tracks deliveries
- `@compliance(id, union, minorsPresent)` - Regulatory compliance
- `@asset_rental(id, assetId, projectId, rate)` - Rents assets
- `@asset_lineage(assetId, history)` - Tracks asset history

### 71-90: Flow/Workflow
- `@flow(id)` - Declares workflow units
- `@retry(str)` - Auto-retry logic
- `@saga(compensate)` - Multi-step transactions
- `@ai(policy)` - AI-driven policies
- `@review(witness)` - Requires witness review
- `@oracle(str)` - Binds external oracles
- `@presence(nexus)` - Verifies Astral-Telluric Grid presence

### 91-100: Interop/Anchoring
- `@import(chain, finality)` - Imports from BTC/ETH/SOL/AR/Sui
- `@abi(cbor_deterministic, version)` - Deterministic ABI
- `@bridge(target)` - Cross-chain bridge

### 101-110: Astral-Telluric Grid
- `@beats(aligned)` - Universal beat alignment
- `@astro(cycle)` - Ephemeris-driven cycles
- `@ley(location)` - Geospatial ley anchors
- `@nexus(anchor)` - Òrìṣà nexus registration
- `@geofence(bounds)` - ZK geofence constraints
- `@boost(multiplier)` - Fee/priority modulation

### 111-120: SatKey/Ordinals
- `@ordinal_guard(sat)` - Ordinal-based access control
- `@inscription(id)` - Blockchain inscriptions
- `@satname(name)` - Unique SatName identifiers
- `@rights(permissions)` - Access rights
- `@playback(replay)` - Execution replay
- `@psbt_policy(rule)` - PSBT policies
- `@bundle(rituals)` - Groups rituals
- `@manifest(metadata)` - Declares metadata

### 121-130: ZK & Numerology
- `@zk(verify, circuit)` - Zero-knowledge proofs
- `@zkbind(ctx)` - Binds ZK to contexts
- `@veil(constant)` - References veil constants (φ, π, etc.)
- `@num` to `@info_bound` - Numerology canon (Veils 1-50)

### 131-143: Economic/Treasury
- `@tithe(rate:369, wallet)` - 3.69% ecosystem tithe
- `@treasury_split(shares)` - AIO wallet splits
- `@shrineSplit(distribution)` - 50/25/15/10 TechGnØŞ splits
- `@fixed_math(precision)` - Fixed-point arithmetic
- `@resource(type)` - Move-inspired resource typing (linear/acquire)
- `@burst(threads)` - Parallel execution
- `@toc(reward, stake, spend)` - Token of Cognition management

### Governance Extensions
- `@nonreentrant` - Prevents reentrant calls
- `@requires(condition)` - Preconditions
- `@ensures(postcondition)` - Postconditions
- `@audit(level)` - Mandates logging
- `@sabbath(day)` - Settle-only on Sabbath
- `@limits(constraints)` - Runtime constraints
- `@align(spec)` - Hardware/temporal alignment
- `@temporal(rule)` - Time-based rules
- `@trusted_caller(address)` - Restricts invocation
- `@sync_guard` - Thread-safe execution
- `@gas_limit(max)` - Caps computational costs
- `@whitegate(quorum)` - Domain protection
- `@oracle_bond(amount)` - Stakes for oracle calls
- `@sector(name)` - Tags sectors for routing

## Usage Examples

### SimaaS Simulation
```
@sim(episode_id:1, steps:1000)
@hardware(estop_check:true)
@accountability(metric:'who_drove_what')
ogun_forge(robot:"unitree_h1") {
    // Simulation ritual
}
```

### AIO Physical Job
```
@job(type:'physical', title:"Drone Delivery", budget:500, requires:pilot_license)
@àṣẹ(proof:qr, witnesses:3, qr:true, auto_device:true)
@sector(name:"logistics")
eshu_router(action:"delivery") {
    // Job ritual
}
```

### TechGnØŞ Offering
```
@offering(amount:369, shrine:"Ọbàtálá_Nexus")
@shrineSplit(distribution:"50/25/15/10")
@tithe(rate:369, wallet:tech_gnosis_treasury)
obatala_guard(blessing:"clarity") {
    // Offering ritual (funds Òrìṣà ROBOTS 100%)
}
```

### Herb Prescription (Òsanyìn)
```
@herb(key:"cerasee", class:"ONCO")
@regional(locale:"DO-Santiago")
@veil(constant:"φ")
osanyin_herb(prescription:"detox") {
    // Herbal ritual
}
```

## Sacred Mathematics

- **Tithe**: Always 369 (3.69%)
- **Vote Weight**: Always 1.0
- **Shrine Split**: 50/25/15/10 (TechGnØŞ only)
- **Veils**: 1-50 numerology constants

## Implementation Status

✅ **Grammar**: All 143 attributes defined in `grammar/ORISA_FULL.pest`  
✅ **Structs**: Complete attribute types in `vm/attributes.go`  
🔄 **Validators**: Core validators implemented  
🔄 **Examples**: Sample rituals in `examples/`

---

**Àṣẹ!** 🔥🕯️

*The canon breathes across all devices: drones, phones, AV, robots, sensors.*
